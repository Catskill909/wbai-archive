node_modules
npm-debug.log
.DS_Store
.env
.env.*
# ...but the documented template of it is committed
!.env.example
*.log
# harvested show-info cache (rebuilt from the live feed)
data/

# Tauri desktop shell (optional, isolated from the zero-dependency web app)
desktop/node_modules/
desktop/src-tauri/target/
desktop/src-tauri/gen/

# feed scanner snapshot (machine-local; rebuilt by test/feed-scan/scan.js)
test/feed-scan/state.json
studio-profile
chrome.log

# local brainstorming doc, never pushed
future-dev.md
